import React from 'react'

import classes from './FlexWrapper.module.css'
import propsMapping from './propsMapping.json'

const FlexWrapper = props => {
    let styles = [classes.FlexWrapper]


    return (
        <div>

        </div>
    )
}

export default FlexWrapper